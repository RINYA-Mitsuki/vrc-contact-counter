name: Validate package
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: python3 -m unittest discover -s tests -v
      - run: python3 scripts/package.py Packages/com.mitsuboshi-studio.vrc-contact-counter --repository "$GITHUB_REPOSITORY"
